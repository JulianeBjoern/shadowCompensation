This folder contains two pyhon notebooks used for any figures made with python. 
To run samtrial.ipynb, the conda environment sam-aerial (samtrial/environment.yml) should be installed.
Weights are downloaded if not already in directory, when running bsub < run_notebook.sh.
